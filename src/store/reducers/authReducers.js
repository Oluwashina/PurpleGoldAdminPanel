
const initState = {
    isAuthenticated: false,
    token: '',
};

const authReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default authReducer